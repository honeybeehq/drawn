{
  "name": "drawn",
  "version": "0.0.1",
  "private": true,
  "description": "Drawn — author AI-generated custom icon sets. Named for drawn comb.",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm --filter @drawn/desktop dev",
    "build": "pnpm -r build",
    "lint": "eslint .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
